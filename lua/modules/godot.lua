-- TODO: set up c# lsp integration with godot so that i can get documentation and not have stuff in the Godot namespace flagged as errors.

-- TODO: write a custom plugin that will allow me to launch scenes from nvim.
-- someone already made one that only works with buffers for gdscript or gsl (T-T), so it should be possible to make one that also works with C#.

-- vim: ts=2 sts=2 sw=2 et
